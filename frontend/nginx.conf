server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  client_max_body_size 100M;

  location / {
    try_files $uri /index.html;
  }

  location /api/ {
    return 404;
  }

  location /assets/ {
    try_files $uri =404;
  }
}
